# torch is installed separately in the Render build command using the CPU-only index:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
# For local dev, install torch normally: pip install torch==2.6.0
torch==2.6.0
fastapi==0.115.12
uvicorn[standard]==0.34.0
transformers==4.51.3
safetensors==0.5.3
accelerate==1.6.0
